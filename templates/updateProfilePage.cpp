#include <bits/stdc++.h>
using namespace std;
pair<int, string> AddHashTagsToProfile()
{
    int x;
    string difference;
    cout << "Enter the tag : ";
    cin >> difference;

    return {x, difference};
}